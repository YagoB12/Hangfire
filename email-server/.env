# === SMTP Mailtrap ===
SMTP_HOST=sandbox.smtp.mailtrap.io
SMTP_PORT=587
SMTP_TLS=true
SMTP_USER=5c350ee7aea78c
SMTP_PASS=9725b0b1265d66
SMTP_FROM="Reportes UCR <noreply@u.cr>"

# Storage Server 
STORAGE_BASE_URL=http://localhost:8001

# URL base del Storage
STORAGE_SERVER_URL=http://localhost:8001

# === Kafka (si no lo estás usando aún, dejalo igual) ===
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
KAFKA_TOPIC=logs-email
SERVICE_NAME=EmailServer
